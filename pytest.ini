[pytest]
testpaths = tests
pythonpath = scripts
addopts = -v --tb=short
markers =
    integration: requires data/strategy/Strategies.xlsx (deselect with -m "not integration")
    slow: long-running tests (deselect with -m "not slow")
